# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# Local Google service account JSON (do NOT commit)
# examples: cointist-cc502bbafba0.json or service-account.json
cointist-*.json
*service-account*.json
*credentials*.json
# ignore downloads on Windows path pattern (editor convenience only)
# Note: this is a generic hint and will not match absolute paths in git; keep local files outside the repo.

# backups and local uploads
backups/
public/uploads/

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel

.env*.local
.vercel
