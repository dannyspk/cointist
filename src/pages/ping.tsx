import React from 'react';

export default function Ping() {
  return (
    <div style={{padding:40,fontFamily:'system-ui,Segoe UI,Roboto',textAlign:'center'}}>
      <h1>ping</h1>
      <p>If you see this page at <code>/ping</code>, Next is serving files from <code>src/pages</code>.</p>
    </div>
  );
}
